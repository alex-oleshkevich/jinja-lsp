{
  "name": "{{ project_name }}",
  "version": "1.0.0",
  {% if include_metadata %}
  "metadata": {
    "created": "{{ created_date }}"
  },
  {% endif %}
  "items": [
    {% for item in items %}
    { "value": "{{ item | lower }}" }{{ "," if not loop.last }}
    {% endfor %}
  ]
}
